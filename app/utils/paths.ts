export enum Paths {
  CREATE_SURVEY = "/surveys/create",
  DASHBOARD = "/dashboard",
  HOME = "/",
  JOIN = "/join",
  LOGIN = "/login",
  PUBLIC = "/public",
  SUCCESS = "/success",
  SURVEYS = "/surveys",
}
